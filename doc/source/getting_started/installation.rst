.. _ref_installation:

Installation
============

Install ``ansys-aedt-mcp`` using ``pip``:

.. code-block:: bash

   pip install ansys-aedt-mcp

Or install from source:

.. code-block:: bash

   git clone https://github.com/ansys/pyaedt-mcp
   cd pyaedt-mcp
   pip install -e .

